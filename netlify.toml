[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"

# Primary deploy is Cloudflare Pages; Netlify kept as optional mirror.
[dev]
  command = "npm run dev"
  port = 4321
  targetPort = 4321
  autoLaunch = false
